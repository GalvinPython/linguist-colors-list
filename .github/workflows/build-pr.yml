# Build package across all maintained/active/current versions of Node.js

name: "CI: Build (PR)"

on:
    pull_request:

concurrency:
    group: node-build-${{ github.ref }}
    cancel-in-progress: true

jobs:
    build:
        runs-on: ubuntu-slim

        strategy:
            fail-fast: false
            matrix:
                node-version: [20, 22, 24, 25]

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: ${{ matrix.node-version }}
                  cache: npm

            - name: Install dependencies
              run: npm ci

            - name: Run tests
              run: npm test

            - name: Build package
              run: npm run build